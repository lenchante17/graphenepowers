# Plan Document Reviewer Prompt Template

This prompt is kept as a thin compatibility wrapper.

In GraphenePowers v4, plan review is normally handled by `graphenepowers:code-review` in `Preflight Spec Review` mode.

Use this file only when a legacy workflow asks for a dedicated plan reviewer and you need a direct prompt template.
