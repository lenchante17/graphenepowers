# Plan Document Reviewer Prompt Template

This prompt is kept as a thin direct-review template.

In GraphenePowers v4, plan review is normally handled by `graphenepowers:code-review` in `Preflight Spec Review` mode.

Use this file only when you need a direct prompt template instead of routing through the unified review skill.
